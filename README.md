Установка модуля происходит из системных модулей
---------------------------------------------------

Использование модуля, это код выводим на странице сайта. 
if (CModule::IncludeModule("formax")) {
    bitrix\Formax\Main::doany();
}

